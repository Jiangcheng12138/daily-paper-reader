---
title: "Brain-DiT: A Universal Multi-state fMRI Foundation Model with Metadata-Conditioned Pretraining"
title_zh: Brain-DiT：一种基于元数据条件预训练的通用多状态 fMRI 基础模型
authors: "Junfeng Xia, Wenhao Ye, Xuanye Pan, Xinke Shen, Mo Wang, Quanying Liu"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12683v1"
tags: ["query:slp-ns"]
score: 9.0
evidence: 在包括睡眠状态在内的多种大脑状态上进行预训练
tldr: 本研究针对现有fMRI基础模型在脑状态多样性和预训练任务上的局限，提出了Brain-DiT。该模型利用涵盖静息、任务、睡眠等多种状态的35万次扫描数据，采用元数据条件的扩散Transformer（DiT）进行预训练。Brain-DiT能够学习捕捉精细功能结构与全局语义的多尺度表征，在7项下游任务中展现出卓越的通用性与性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12683v1/fig-001.webp\", \"caption\": \"Table 2. Downstream performance with a frozen backbone. Best results are shown in red; underlined values denote the runner-up. * indicates p < 0.05 vs. the best baseline.\", \"page\": 6, \"index\": 1, \"width\": 600, \"height\": 282}, {\"url\": \"assets/figures/arxiv/2604.12683v1/fig-002.webp\", \"caption\": \"Fig. 1. Brain-DiT framework: pretraining and downstream adaptation. (a) Pretraining. An ROI time-series window is noised at a random timestep t and denoised by a conditional/unconditional DiT using v-prediction. (b) Downstream adaptation. Multiple noisy views at selected timesteps are processed by the shared pretrained DiT. Token features from selected layers are aggregated to form a representation for downstream classification or regression.\", \"page\": 2, \"index\": 2, \"width\": 725, \"height\": 483}, {\"url\": \"assets/figures/arxiv/2604.12683v1/fig-003.webp\", \"caption\": \"Fig. 3. State diversity ablation and contribution analysis. (a) State diversity ablation. Mixtures of multi-state fMRI data are used during pretraining with a fixed pretraining budget (∼30k sessions): R (resting), +D (disease), +T (task), +N (naturalistic), and ALL (the full multi-state corpus). (b) Contribution analysis. Aggregator weights show contributions across timesteps (left) and network depth (right), reflecting global (later/noisier) vs. local (earlier/cleaner) features.\", \"page\": 7, \"index\": 3, \"width\": 725, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2604.12683v1/fig-004.webp\", \"caption\": \"Fig. 2. Metadata-conditioned generation of Brain-DiT. (a) Original vs. generated fMRI signals. Generated fMRI signals exhibit similar spatiotemporal patterns to the original fMRI. (b) Group-level FC comparison. Empirical and synthetic group-level FC matrices for 200 metadata-conditioned virtual ASD subjects exhibit similar large-scale connectivity organization. (c) FC error metrics. MAE and RMSE (lower is better) across three settings: random signal, unconditional generation, and metadata-conditioned generation. All results are computed on the ABIDE dataset.\", \"page\": 3, \"index\": 4, \"width\": 725, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2604.12683v1/fig-005.webp\", \"caption\": \"Table 1. Downstream performance on in-distribution (ID) and out-ofdistribution (OOD) tasks. Top: ID; bottom: OOD. Brain-DiTAAL424 and BrainDiTuncond are ablations. Best and runner-up results are highlighted in red and by underlining, respectively. * indicates p < 0.05 vs. the best baseline (BrainLM/BrainJEPA/BrainMass); no tests are performed among our variants.\", \"page\": 4, \"index\": 5, \"width\": 723, \"height\": 488}]"
motivation: 现有fMRI基础模型因训练数据状态单一且预训练任务与表征学习不匹配，限制了其在多样化脑状态下的泛化能力。
method: 提出一种基于元数据条件的扩散Transformer预训练框架，通过生成式扩散任务学习多尺度的fMRI神经表征。
result: 实验证明扩散式预训练优于传统的重建或对齐任务，且元数据条件能有效分离内在神经动力学与群体水平的变异性。
conclusion: Brain-DiT作为通用的多状态fMRI基础模型，通过扩散预训练显著提升了下游任务表现，并揭示了不同任务对表征尺度的特定偏好。
---

## 摘要
目前的 fMRI 基础模型主要依赖于有限的脑状态范围和不匹配的预训练任务，限制了它们在不同脑状态下学习通用表征的能力。我们提出了 Brain-DiT，这是一个通用的多状态 fMRI 基础模型，在来自 24 个数据集的 349,898 个会话（sessions）上进行了预训练，涵盖了静息态、任务态、自然态、疾病态和睡眠态。与以往依赖原始信号空间或潜空间掩码重构的 fMRI 基础模型不同，Brain-DiT 采用了基于扩散 Transformer (DiT) 的元数据条件扩散预训练，使模型能够学习捕捉细粒度功能结构和全局语义的多尺度表征。通过对 7 个下游任务的广泛评估和消融实验，我们一致发现，基于扩散的生成式预训练是比重构或对齐更强大的代理任务，而元数据条件预训练通过将内在神经动力学与群体水平的变异性解耦，进一步提升了下游性能。我们还观察到，下游任务对表征尺度表现出明显的偏好：ADNI 分类更多地受益于全局语义表征，而年龄/性别预测则相对更依赖于细粒度的局部结构。Brain-DiT 的代码和参数可在 https://github.com/REDMAO4869/Brain-DiT 获取。

## Abstract
Current fMRI foundation models primarily rely on a limited range of brain states and mismatched pretraining tasks, restricting their ability to learn generalized representations across diverse brain states. We present \textit{Brain-DiT}, a universal multi-state fMRI foundation model pretrained on 349,898 sessions from 24 datasets spanning resting, task, naturalistic, disease, and sleep states. Unlike prior fMRI foundation models that rely on masked reconstruction in the raw-signal space or a latent space, \textit{Brain-DiT} adopts metadata-conditioned diffusion pretraining with a Diffusion Transformer (DiT), enabling the model to learn multi-scale representations that capture both fine-grained functional structure and global semantics. Across extensive evaluations and ablations on 7 downstream tasks, we find consistent evidence that diffusion-based generative pretraining is a stronger proxy than reconstruction or alignment, with metadata-conditioned pretraining further improving downstream performance by disentangling intrinsic neural dynamics from population-level variability. We also observe that downstream tasks exhibit distinct preferences for representational scale: ADNI classification benefits more from global semantic representations, whereas age/sex prediction comparatively relies more on fine-grained local structure. Code and parameters of Brain-DiT are available at \href{https://github.com/REDMAO4869/Brain-DiT}{Link}.

---

## 论文详细总结（自动生成）

### Brain-DiT：基于元数据条件预训练的通用多状态 fMRI 基础模型总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
人类大脑是一个在不同状态（如静息、认知任务、睡眠等）间动态切换的复杂系统。现有的 fMRI 基础模型（Foundation Models）面临三大局限：
*   **预训练分布受限**：大多仅在静息态数据上训练，缺乏对全谱系大脑动态的覆盖。
*   **代理任务不匹配**：传统的掩码自编码（MAE）或对比学习可能过拟合于低信噪比的原始信号，或面临表征坍缩风险。
*   **元数据利用不足**：忽略了临床和人口统计学元数据（如年龄、性别、诊断），导致模型难以将内在神经动力学与群体变异性解耦。
**Brain-DiT** 旨在通过大规模多状态数据和生成式扩散模型，构建一个能捕捉多尺度特征的通用 fMRI 基础模型。

#### 2. 论文提出的方法论
*   **核心思想**：利用扩散 Transformer（DiT）作为骨干网络，通过元数据条件的生成式任务进行预训练，将去噪过程视为分层特征提取。
*   **关键技术细节**：
    *   **扩散预训练**：采用 DDPM 前向加噪过程和 $v$-prediction 目标函数。将 ROI 时间序列切片并 Patch 化，输入 DiT 进行去噪。
    *   **元数据条件化**：通过 AdaLN-Zero 模块将年龄、性别、疾病诊断等元数据注入 DiT 的每个 Block，实现条件化生成，从而解耦个体差异。
    *   **多尺度特征聚合**：在下游任务中，不只使用最后一层输出，而是从多个扩散步（Timesteps）和多个 Transformer 层提取特征，通过一个可学习的查询（Query-based）聚合模块进行融合。
    *   **生成式代理任务**：相比重构任务，扩散模型能学习到更丰富的概率流形，早期步长捕捉细粒度结构，后期步长编码抽象全局语义。

#### 3. 实验设计
*   **数据集**：使用了 24 个多状态数据集，包含 349,898 个会话。涵盖静息态、任务态、自然刺激（电影/音乐）、疾病态（ADHD/自闭症/帕金森）和睡眠态。
*   **Benchmark 与对比方法**：
    *   **分布内（ID）任务**：HCP（性别分类）、ABIDE-AGE（年龄预测）、SALD（年龄预测）。
    *   **分布外（OOD）任务**：PPMI（帕金森分类）、ADHD（多动症分类）、NKI（教育程度/年龄预测）。
    *   **对比基准**：BrainLM（基于 MAE）、Brain-JEPA、BrainMass（基于对比学习）、SlimBrain 等主流 fMRI 基础模型。

#### 4. 资源与算力
*   **硬件**：使用了 **6 台 NVIDIA A800 (80GB) GPU**。
*   **训练细节**：预训练进行了 150 个 Epoch，采用 AdamW 优化器，混合精度训练。Batch size 为每张显卡 36。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 7 个下游任务上进行了广泛评估，涵盖了分类和回归。
*   **消融实验**：包括了针对元数据条件（uncond vs. cond）、分区方案（AAL424 vs. Schaefer-1000）、以及大脑状态多样性（R, +D, +T, +N, ALL）的对比。
*   **充分性评价**：实验设计非常充分且客观。通过冻结骨干网络（Frozen Backbone）的线性评估验证了表征的纯粹质量，并利用 OOD 数据集严格测试了模型的泛化能力。

#### 6. 论文的主要结论与发现
*   **性能领先**：Brain-DiT 在所有 ID 和 OOD 任务中均显著优于现有基准模型。
*   **状态多样性的价值**：实验证明，下游任务性能的提升主要源于预训练数据中脑状态的多样性，而非单纯的数据量增加。
*   **元数据的重要性**：引入元数据条件能显著提升生成质量和下游任务的表征能力。
*   **任务偏好差异**：发现不同任务对特征尺度有偏好，例如阿尔兹海默症（ADNI）分类更依赖后期扩散步的全局语义，而年龄预测则需要更细粒度的局部结构。

#### 7. 优点
*   **通用性强**：首个统一了静息、任务、睡眠等多种状态的 fMRI 基础模型。
*   **多尺度表征**：巧妙利用扩散模型的去噪轨迹提取分层特征，提升了模型的解释性。
*   **生成能力**：不仅能做表征学习，还能生成高质量、符合群体统计特征的虚拟 fMRI 信号。

#### 8. 不足与局限
*   **计算开销**：扩散模型的迭代推理过程相比单次前向传播的 MAE 模型计算成本更高。
*   **元数据依赖**：虽然元数据提升了性能，但在某些临床场景下，高质量的元数据可能难以获取或存在缺失。
*   **序列长度限制**：目前主要针对 ROI 时间序列窗口（T=40），对于超长序列或极高空间分辨率的原始体素处理仍面临扩展性挑战。

（完）
