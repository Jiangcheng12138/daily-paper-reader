---
title: Sleep-stage efficient classification using a lightweight self-supervised model
title_zh: 使用轻量级自监督模型的睡眠阶段高效分类
authors: "Eldiane Borges dos Santos Durães, João Batista Florindo"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26295v1"
tags: ["query:slp-ns"]
score: 9.0
evidence: 提出轻量级自监督模型用于睡眠分期分类，推动自动睡眠分析发展
tldr: 睡眠阶段准确分类对诊断睡眠障碍至关重要，自动化可提升临床效率。本文采用简化自监督模型mulEEG（ResNet-18代替ResNet-50）提取脑电特征，结合线性SVM分类器。通过减少训练数据量和特征优化，发现拼接时间序列与频谱特征相比原模型线性评估取得更高分类性能。该方法在轻量级模型上实现高效准确分类，为临床睡眠评估提供实用方案，并可扩展到其他生物信号。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26295v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 795, \"height\": 246, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26295v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1409, \"height\": 888, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26295v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 794, \"height\": 303, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.26295v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1193, \"height\": 687, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.26295v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1165, \"height\": 523, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.26295v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1403, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.26295v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1144, \"height\": 448, \"label\": \"Table\"}]"
motivation: 准确睡眠阶段分类对诊断睡眠障碍至关重要，现有模型复杂度高，需探索轻量高效自动化方法。
method: 采用简化自监督mulEEG模型（ResNet-18骨干）提取脑电特征，以线性SVM分类，并评估不同数据量与特征组合。
result: 减少训练数据量性价比更优，拼接时间序列与频谱特征分类性能超越原模型线性评估。
conclusion: 简化模型配合鲁棒分类器实现高效睡眠阶段分类，具有临床推广价值。
---

## 摘要
准确分类睡眠阶段对于诊断睡眠障碍至关重要，自动化该过程可显著提升临床评估效率。本研究旨在探索将自监督模型（具体而言，为mulEEG的改进版本）与线性SVM分类器相结合，以改善睡眠阶段分类。\textbf{方法：} mulEEG模型以自监督方式学习脑电图信号表示，本文对其进行了简化：将原本作为时间序列编码器的ResNet-50替换为基于1D卷积的ResNet-18骨干网络。此外进行了两项调整：第一项评估了不同模型配置与训练数据量，第二项测试了时间序列特征、谱图特征及其拼接作为线性SVM分类器输入的有效性。\textbf{结果：}结果表明，相较于简化模型，减少数据量能提供更优的性价比。使用ResNet-18的拼接特征也优于原始mulEEG模型的线性评估，实现了更高的分类性能。\textbf{结论：} 简化mulEEG模型以提取特征，并将其与鲁棒分类器结合，可实现更高效、更准确的睡眠阶段分类。该方法有望改进临床睡眠评估，并可推广至其他生物信号分类任务。

## Abstract
Accurate classification of sleep stages is crucial for diagnosing sleep disorders and automating this process can significantly enhance clinical assessments. This study aims to explore the use of a self-supervised model (more specifically, an adapted version of mulEEG) combined with a Linear SVM classifier to improve sleep stage classification. \textbf{Methods:} The mulEEG model, which learns electroencephalogram signal representations in a self-supervised manner, was simplified here by replacing ResNet-50 with 1D-convolutions used as time series encoder by a ResNet-18 backbone. Two other adaptations were conducted: the first one evaluated different configurations of the model and data volume for training, while the second tested the effectiveness of time series features, spectrogram features, and their concatenation as inputs to a Linear SVM classifier. \textbf{Results:} The results showed that reducing the volume of data offered a better cost-benefit ratio compared to simplifying the model. Using the concatenated features with ResNet-18 also outperformed the linear evaluations of the original mulEEG model, achieving higher classification performance. \textbf{Conclusions:} Simplifying the mulEEG model to extract features and pairing it with a robust classifier leads to more efficient and accurate sleep stage classification. This approach holds promise for improving clinical sleep assessments and can be extended to other biological signal classification tasks.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：准确睡眠阶段分类对于诊断睡眠障碍至关重要，但现有的自监督深度学习模型（如 mulEEG）虽然性能优异，却存在模型复杂、计算成本高、依赖大量标注或未标注数据的问题。尤其在数据量和算力受限的临床或资源有限场景下，直接应用这些模型不现实。因此，本文旨在探索一种 **轻量级、高效的自监督模型**，在保持或提升分类性能的同时，显著降低计算开销。
- **整体意义**：通过简化模型（用 ResNet-18 替代 ResNet-50）并搭配更强健的分类器（线性 SVM），实现了比原始 mulEEG 线性评估更优的分类效果，且训练时间大幅缩短。这为构建资源高效的自动睡眠分期系统提供了可行方案，并可推广至其他生物信号分类任务（如心电图异常检测）。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 以 mulEEG 为基线，将其时间序列编码器从 ResNet-50（1D卷积）替换为更轻量的 **ResNet-18（1D卷积）**，从而降低模型参数量和训练时间。
- 在预训练阶段，同时实验 **20% 和 100% 的预训练数据量**，以评估数据量对效果的影响。
- 在下游分类阶段，放弃原始 mulEEG 中的简单线性分类器，改用 **线性 SVM**，并输入不同特征（时间序列特征、频谱特征、两者拼接），以充分挖掘自监督学习到的表示能力。

### 关键技术细节
1. **自监督预训练（mulEEG 框架）**：
   - 通过数据增强生成两个视图：T1（抖动+掩码）得到时间序列 t1，T2（随机翻转+高斯噪声缩放）得到 t2；再将它们转换为频谱图 s1、s2。
   - 时间序列编码器 Et（ResNet-50 或 ResNet-18，1D卷积）和频谱编码器 Es（保持不变）分别提取特征。
   - 投影头（全连接网络）将特征映射到对比空间，使用 **NT-Xent 对比损失** 和 **多样性损失 LD** 进行训练。
   - 总损失：\( L_{tot} = \lambda_1 (L_{TT} + L_{FF} + L_{SS}) + \lambda_2 L_D \)，其中 \(\lambda_1=1, \lambda_2=2\)，温度参数 \(\tau=1, \tau_d=10\)。

2. **下游分类**：
   - 冻结预训练好的 Et 和 Es，提取时间序列特征、频谱特征或两者拼接作为输入。
   - 使用 **线性 SVM**（squared hinge loss，C=1，tol=1e-4，max_iter=1000，多类采用 one-vs-rest 策略）进行分类。

3. **模型架构简化**：
   - ResNet-50 with 1D conv：4个卷积阶段，通道数 32→64→128→256，滤波器大小 25，全局均值池化后输出 256维。
   - ResNet-18 with 1D conv：类似结构但更浅，通道数 8→16→32→64，每个阶段2个残差块。

## 3. 实验设计：数据集、基准、对比方法

### 数据集
- **Sleep-EDF 数据库**：153 个整夜多导睡眠图记录（PSG + 睡眠分期标注），采样率 100 Hz。
- 预处理：与原始 mulEEG 保持一致，选择 58 个患者作为无标签预训练组，20 个用于线性评估（5折交叉验证）。每个 epoch 为 30 秒（3000 个采样点）。

### 基准（Benchmark）
- 原始 mulEEG 的线性评估结果（来自 Kumar et al., 2022）：
  - Accuracy = 0.7806，Cohen's Kappa = 0.6850，Macro F1 = 0.6782。

### 对比方法
- **第一组实验（线性评估）**：比较四种配置（数据量 × 编码器）：
  - 20% 数据 + ResNet-18
  - 20% 数据 + ResNet-50
  - 100% 数据 + ResNet-18
  - 100% 数据 + ResNet-50
  - 与原始 mulEEG 线性评估（ResNet-50 + 100%数据，但 epoch 数不同）对比。
- **第二组实验（线性 SVM 分类）**：使用预训练最好的编码器（来自第一组中 MF1 最高的 checkpoint），改变 SVM 输入：
  - 原始 EEG 信号（作为基线对照）
  - 时间序列特征（Et 输出）
  - 频谱特征（Es 输出）
  - 拼接特征（两者拼接）
  - 分别使用 ResNet-18 和 ResNet-50 的 Et。

### 评估指标
- Accuracy (Acc)、Cohen's Kappa (κ)、Macro-averaged F1 (MF1)，并记录了训练时间。

## 4. 资源与算力

文中明确给出了计算环境与训练时长：

- **硬件**：Intel Core i7-8700，16 GB RAM，Nvidia Titan V 显卡。
- **软件**：Python 3.9 / PyTorch 2.5，Linux Ubuntu 24.10。
- **训练时长（预训练 + 线性评估）**：
  - 20%数据 + ResNet-18：约 3 小时 2 分 55 秒
  - 20%数据 + ResNet-50：约 4 小时 27 分 26 秒
  - 100%数据 + ResNet-18：约 13 小时 21 分 22 秒
  - 100%数据 + ResNet-50：约 21 小时 2 分 56 秒
- **注意**：预训练 epoch 设为 200（原始 mulEEG 为 140），从 epoch 80 开始每 4 个 epoch 进行一次线性评估，线性分类器训练 100 epoch。SVM 训练时间未单独列出。

## 5. 实验数量与充分性

- **实验数量**：两组实验共计约 4（第一组）+ 8（第二组）种配置，覆盖了数据量、编码器复杂度、分类器类型、特征组合四个维度的消融。
- **充分性与公平性**：
  - 使用同一数据集、相同预处理，对比条件明确。
  - 但仅有一个数据集（Sleep-EDF），泛化性受限。
  - 未与其他最新睡眠分期方法（如 supervised 深度学习模型或其他自监督方法）进行对比，仅与原始 mulEEG 线性评估比较。
  - 预训练 epoch 数不同（200 vs 140）可能带来一定不公平，但作者解释是为了充分训练轻量模型。
  - 未报告统计显著性（如置信区间或 p 值），仅给出一次性运行结果（可能由于 5 折交叉验证取平均？但未说明方差）。
  - 未探索超参数（如 SVM 的 C 值、对比损失的温度参数等）的影响。

总体而言，实验设计合理、消融充分，但范围偏窄，缺乏多数据集验证和与 SoTA 方法的全面对比。

## 6. 主要结论与发现

1. **减少数据量比简化模型性价比更高**：使用 ResNet-50 + 20% 数据（4h27m）的指标（Acc 0.7483）与 ResNet-18 + 100% 数据（13h21m）的指标（Acc 0.7549）接近，但时间节省约 2/3。
2. **线性 SVM 优于简单的线性分类器**：即使使用 ResNet-18，时间序列特征输入 SVM 的 Acc 0.7732 已接近原始 mulEEG 的 0.7806；拼接特征则超越（Acc 0.7909）。
3. **频谱特征单独使用效果较差**，但与时间序列拼接后可提供互补信息，特别是在轻量编码器（ResNet-18）下提升明显。
4. **随着时间序列编码器复杂度增加（ResNet-50）**，频谱特征的贡献减弱，与时间序列特征单独使用或拼接结果相近（MF1 0.7239 vs 0.7373）。
5. **最优秀的配置是 ResNet-50 + 拼接特征（MF1 0.7373）**，但 ResNet-18 + 拼接特征（MF1 0.6972）也超过了原始 mulEEG，且训练时间更短，体现了更好的成本效益。

## 7. 优点：方法或实验设计上的亮点

- **轻量高效**：直接替换骨干网络、减少数据量，探索了实际部署中的资源约束。
- **分类器改进**：将线性分类器升级为线性 SVM，能在不增加大量计算的前提下显著提升分类性能。
- **特征融合视角**：系统比较了时间序列、频谱及其拼接效果，揭示了不同表示之间的互补性，为后续特征选择提供参考。
- **实验设计清晰**：两组实验分别考察自监督阶段和下游分类阶段，消融逻辑直观。
- **开源与可复现**：PyTorch 实现，训练细节充足，便于复现。

## 8. 不足与局限

- **数据集单一**：仅使用 Sleep-EDF 一个数据集，未在更大规模或更多样化的数据集（如 MASS、SHHS）上验证，模型泛化性存疑。
- **缺少与最新方法的全面对比**：未与近年来先进的睡眠分期方法（如 AttnSleep、SleepTransformer、基于对比学习的其他 SSL 方法）进行比较，仅对比了原始 mulEEG。
- **实验偏差风险**：预训练 epoch 数（200 vs 140）不一致，可能给轻量模型更多优化机会；SVM 的参数（C=1）未调优，可能不是最优。
- **未报告可变性**：虽然用了 5 折交叉验证，但表 3、表 4 中未列出标准差或置信区间，无法判断结果的稳定性。
- **未讨论推理成本**：仅关注训练时间，未给出推理速度或模型参数量等轻量性指标。
- **临床应用限制**：分类任务基于 30 秒 epoch，未考虑睡眠阶段之间的时序依赖性，实际应用可能需要序列模型（如 RNN/Transformer）进一步优化。
- **仅针对 EEG 单一模态**：实际多导睡眠图包含多种生理信号，本文仅使用 EEG，可能遗漏互补信息。

（完）
